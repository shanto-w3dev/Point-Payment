Create a simple point based payment gateway for woocommerce, so that Admin can enable it. Then create a minimal admin panel where admin can select users and add points. Also, admin will be able to set the point conversion rate (Like 1 point = 1 currency). Then on the chekout page, it will show pay with  point. In the woocommerce user frontend dashboard, it will current point and point history. Use OOP and split the code it diffrent files. Keep the pont conversion settings in the point gateway ssetting page. Keep the code biginner friendly.